name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - run: flutter pub get

      # base-href matches this repo's name so the app works both at this
      # repo's own project-pages URL (curatedcontent.github.io/SaiKirthan/)
      # and at the same subpath in the catalogentry-hub site
      # (www.catalogentry.com/SaiKirthan/).
      - run: flutter build web --release --base-href /SaiKirthan/ --no-web-resources-cdn

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

      # Also publish into curatedcontent/catalogentry-hub's gh-pages branch
      # under /SaiKirthan/, so www.catalogentry.com/SaiKirthan/ stays in
      # sync. keep_files preserves the hub's own content and other projects.
      - uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ secrets.HUB_DEPLOY_TOKEN }}
          external_repository: curatedcontent/catalogentry-hub
          publish_branch: gh-pages
          publish_dir: build/web
          destination_dir: SaiKirthan
          keep_files: true
          user_name: Claude
          user_email: noreply@anthropic.com

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
